---
import { configLoader } from "../utils/configLoader";
const CONFIG = await configLoader();

const logo_text = CONFIG.LOGO_TEXT ?? CONFIG.TITLE;
---

<a
  href="/"
  class="boxed shadow-[3px_3px_0px_0px] px-2 py-1 border-2 text-2xl font-bold"
  >{logo_text}</a
>
